[project]
name = "deevyanshoo-profile"
version = "1.0.0"
description = "Deterministic, privacy-safe GitHub profile telemetry and SVG renderer"
requires-python = ">=3.12"
license = "MIT"
authors = [{name = "Divyanshu Goyal"}]

[project.optional-dependencies]
portrait = ["Pillow==12.3.0"]

[tool.unittest]
start-directory = "tests"
